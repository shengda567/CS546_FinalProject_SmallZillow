<div>
  {{#if error}}
  <div class="error">
      {{error}}
  </div>
  {{/if}}
<div>
<main>
  <ul>
    {{#each posts}}
      <li><a href="/posts/{{_id}}">{{title}}</a></li>

    {{/each}}
  </ul>
</main>
